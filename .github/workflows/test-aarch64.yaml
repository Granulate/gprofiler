name: Aarch64 test

on: pull_request

jobs:
  aarch64:
    runs-on:
      - self-hosted
      - gprofiler
      - ARM64

    steps:
    - name: Install dependencies
      run: |
        uname -a
        uname -m

  x86:
    runs-on:
      - self-hosted
      - gprofiler
      - X86

    steps:
    - name: Install dependencies
      run: |
        uname -a
        uname -m
